{
  "name": "jcdn",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "main": "index.ts",
  "license": "Apache-2.0",
  "scripts": {
    "watch": "bun .node/swc.cjs --bun --watch",
    "compile": "bun .node/swc.cjs --bun --compile",
    "reset": "bun .node/reset.cjs --bun --reset",
    "fix": "bun .node/fix.cjs --bun --fix",
    "gcloud": "bun .node/gcloud.cjs --bun --deploy",
    "vsce": "bun .node/vsce.cjs --bun --package"
  },
  "devDependencies": {
    "@types/clean-css": "^4.2.11",
    "@types/node": "^24.10.1"
  },
  "dependencies": {
    "clean-css": "^5.3.3"
  }
}
